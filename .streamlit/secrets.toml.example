# Copy to secrets.toml locally (gitignored) and set in Streamlit Cloud
# app settings -> Secrets. Optional second gate on top of the private-repo
# viewer allowlist. Leave unset to disable the password prompt.
app_password = "change-me"
